# subagent/

放多 Agent 协作实验。

适合放：

- planner / executor / reviewer 模式
- researcher / writer / critic 模式
- supervisor-worker 模式
- 多 Agent 消息协议
- 成本、延迟、失败传播分析

注意：多 Agent 不是默认更好。只有当任务确实需要角色隔离、并行处理、独立评审或复杂状态隔离时才值得使用。

