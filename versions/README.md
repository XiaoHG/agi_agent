# versions/

这个目录用于存放版本化的迭代说明文件。

命名规范：

```text
v01_<topic>.md
v02_<topic>.md
```

- 版本号必须前置，便于按数字排序。
- 同一个版本只对应一个主功能主题。
- 版本说明文件应与 `docs/vNN_*.md` 练习文档保持同号对应。

每个版本文件都应该说明：

- 本次改了什么
- 新增或修改了哪些文件
- 新代码对应的行号
- 新增了什么行为和交互流程
- 验证结果如何
